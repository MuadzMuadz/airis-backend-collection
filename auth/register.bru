meta {
  name: register
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "newuser2",
    "email": "muadz.mail18@gmail.com",
    "password": "securepassword123",
    "full_name": "New User 2"
  }
}

script:post-response {
  const data = res.getBody();
  
  if (data && data.id) {
    bru.setEnvVar("user_id", data.id);
    console.log("Registered user_id saved:", data.id);
  }
}
