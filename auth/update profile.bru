meta {
  name: update profile
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/auth/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  "username": "newusername",
  "email": "newemail@example.com",
  "full_name": "New Full Name"
}
