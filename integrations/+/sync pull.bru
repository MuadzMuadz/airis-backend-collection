meta {
  name: Sync Pull
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/integrations/zotero/sync/pull
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "zotero_collection_key": "",
    "target_folder_id": "{{folder_id}}"
  }
}
