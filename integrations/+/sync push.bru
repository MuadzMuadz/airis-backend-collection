meta {
  name: Sync Push
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/integrations/zotero/sync/push
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "project_id": "{{project_id}}"
  }
}
