meta {
  name: Sync Pull
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/integrations/zotero/sync/pull
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "project_folder_id": "{{folder_id}}",
    "zotero_collection_key": ""
  }
}
