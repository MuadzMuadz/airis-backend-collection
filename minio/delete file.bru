meta {
  name: delete file
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/minio/delete/{{filename}}?session_id={{session_id}}
  body: none
  auth: bearer
}

params:query {
  session_id: {{session_id}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
