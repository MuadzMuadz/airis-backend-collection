meta {
  name: list files
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/minio/list/{{session_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
