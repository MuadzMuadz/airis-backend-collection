meta {
  name: upload file
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/minio/upload
  body: json
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
{
    "name": "project 1",
    "description": "test"
  }
}
