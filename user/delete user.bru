meta {
  name: delete user
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/v1/users/{{user_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
